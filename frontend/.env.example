# Optional same-origin prefix. Keep empty when the frontend and API share an origin.
VITE_API_BASE_URL=

# Optional development-only proxy target, for example http://127.0.0.1:8080.
VITE_API_PROXY_TARGET=
